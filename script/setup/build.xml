<?xml version="1.0" ?>
<!-- $Id: build.xml shyam $ -->
<project name="JSPT Component" basedir="." default="setup" description="Phing setup script for JSPT package.">

	<target name="config" description="Load configuration file">
		<property file="../build/build.properties"  override="true" />		
		<svnlastrevision workingcopy="${global.dir.root}" propertyname="svn.lastrevision" />
		<php expression="rand(1000,9999)" returnProperty="joomla.now"/>
		<php expression="rand(100,999)" returnProperty="joomla.random3"/>
		<property file="build.properties"  override="false" />
	</target>
	


	<target name="setup" description="build installable package only" depends="config">
		<phing phingfile="joomla.xml" inheritAll="true" target="setup" />
		<phingcall target="copy_com_xipt" />
		<phingcall target="copy_plg_xipt_system" />
		<phingcall target="copy_plg_xipt_community" />
		<phingcall target="copy_mod_xiptmembers" />
		<phingcall target="copy_testcase" />
		<!-- Copy latest JomSocial -->
		<copy 	file="${pkg.location}/com_community.zip" 
				tofile="${joomla.rootpath}/${joomla.folder}/com_community.zip" />

		<copy 	file="${pkg.location}/jomsocialApps/plg_feeds.zip" 
				tofile="${joomla.rootpath}/${joomla.folder}/plg_feeds.zip" />
		<copy 	file="${pkg.location}/jomsocialApps/plg_groups.zip" 
				tofile="${joomla.rootpath}/${joomla.folder}/plg_groups.zip" />
		<copy 	file="${pkg.location}/jomsocialApps/plg_latestphoto.zip" 
				tofile="${joomla.rootpath}/${joomla.folder}/plg_latestphoto.zip" />
		<copy 	file="${pkg.location}/jomsocialApps/plg_myarticles.zip" 
				tofile="${joomla.rootpath}/${joomla.folder}/plg_myarticles.zip" />

		<copy 	file="${pkg.location}/jomsocialApps/testing001.tar.gz" 
				tofile="${joomla.rootpath}/${joomla.folder}/plg_testing001.tar.gz" />
		<copy 	file="${pkg.location}/jomsocialApps/testing002.tar.gz" 
				tofile="${joomla.rootpath}/${joomla.folder}/plg_testing002.tar.gz" />

		<copy 	file="${global.dir.root}/script/others/revbuild.xml" 
				tofile="${joomla.rootpath}/${joomla.folder}/build.xml" />

		<exec command="sudo chmod -R  ." dir="${joomla.rootpath}/${joomla.folder}" />
		<!-- Replace current location path in revbuild.xml for replacing the code -->
		<reflexive>
			<fileset dir="${joomla.rootpath}/${joomla.folder}">
			    <include name="build.xml" />
			</fileset>
			 <filterchain>
				<replacetokens>
				    <token key="local.repository.location" 	value="${project.basedir}/../../"/>
				</replacetokens>
			</filterchain>
		</reflexive>

	</target>

	<target name="copy_testcase">
		<property file="../build/build.properties"  override="true" />
		<!-- Copy testcases -->
		<copy todir="${joomla.rootpath}/${joomla.folder}/test">
			<fileset dir="${global.dir.root}/test">
			     <include name="**/*.*" />
			</fileset>
		</copy>
		<exec command="sudo chmod -R  test" dir="${joomla.rootpath}/${joomla.folder}" />
		<!-- replace configuration-->
		<reflexive>
			<fileset dir="${joomla.rootpath}/${joomla.folder}/test">
			    <include name="system/jdefines.php" />
			</fileset>
			 <filterchain>
				<replacetokens>
				    <token key="joomla.folder" 	value="${joomla.folder}"/>
				</replacetokens>
			</filterchain>
		</reflexive>
	</target>

	<target name="copy_plg_xipt_community">
		<property file="../build/build.properties"  override="true" />
		<property file="../build/plg_xipt_community.properties"  override="true" />
		<echo msg="Copying ${dir.packages}/${file.package}${svn.lastrevision}${file.extension} " />
		<copy 	file="${dir.packages}/${file.package}${svn.lastrevision}${file.extension}" 
				tofile="${joomla.rootpath}/${joomla.folder}/${project.name}${file.extension}" />
	</target>

	<target name="copy_com_xipt">
		<property file="../build/build.properties"  override="true" />

		<property file="../build/com_xipt.properties"  override="true" />	
		<echo msg="Copying ${dir.packages}/${file.package}${svn.lastrevision}${file.extension} " />
		<copy 	file="${dir.packages}/${file.package}${svn.lastrevision}${file.extension}" 
				tofile="${joomla.rootpath}/${joomla.folder}/${project.name}${file.extension}" />
		
	</target>

	<target name="copy_plg_xipt_system">
		<property file="../build/build.properties"  override="true" />

		<property file="../build/plg_xipt_system.properties"  override="true" />
		<echo msg="Copying ${dir.packages}/${file.package}${svn.lastrevision}${file.extension} " />
		<copy 	file="${dir.packages}/${file.package}${svn.lastrevision}${file.extension}" 
				tofile="${joomla.rootpath}/${joomla.folder}/${project.name}${file.extension}" />
	</target>

    <target name="copy_mod_xiptmembers">
		<property file="../build/build.properties"  override="true" />

		<property file="../build/mod_xiptmembers.properties"  override="true" />
		<echo msg="Copying ${dir.packages}/${file.package}${svn.lastrevision}${file.extension} " />
		<copy 	file="${dir.packages}/${file.package}${svn.lastrevision}${file.extension}" 
				tofile="${joomla.rootpath}/${joomla.folder}/${project.name}${file.extension}" />
	</target>
</project>
