1. Checkout latest code from SVN ( you can specify branch/version)
2. Build Kit
	a. Apply Code Sniffer
	b. Apply Code standard
	c. Create Zips
3. Setup
	a. Install Joomla (default latest)
	b. Install JomSocial (default latest)
	c. Install our builded component & plugins
4. Fire tests
	a. Unit Testcase
	b. Interface Test
5. Send results via  email.
